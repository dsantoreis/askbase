apiVersion: networking.k8s.io/v1
kind: Ingress
metadata: {name: askbase-docs}
spec:
  rules:
    - host: docs.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: askbase-docs
                port:
                  number: 80
